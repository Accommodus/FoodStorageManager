{
  "name": "@foodstoragemanager/server",
  "type": "module",
  "dependencies": {
    "@foodstoragemanager/schema": "0.1.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "http-status-codes": "^2.3.0"
  },
  "scripts": {
    "build:scheme": "cd ../schema && npm run build",
    "build": "npm run build:scheme && tsc -p tsconfig.json",
    "dev": "npm run build:scheme && tsx src/index.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
