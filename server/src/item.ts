import { ApiHandler } from "./types.js";
import { UserModel } from "@foodstoragemanager/schema";

export const createItem: ApiHandler = (req, res, db) => {
    // will create item based on request

    let valid = UserModel()
}
