{
  "name": "foodstoragemanager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "npm run schema && concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run schema && npm --workspace=server run build && npm --workspace=client run build",
    "start": "npm run build && npm --workspace=server run start",
    "client": "npm --workspace=client run dev",
    "server": "npm --workspace=server run dev",
    "schema": "npm --workspace=schema run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Accommodus/FoodStorageManager.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Accommodus/FoodStorageManager/issues"
  },
  "homepage": "https://github.com/Accommodus/FoodStorageManager#readme",
  "workspaces": [
    "server",
    "client",
    "schema"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
