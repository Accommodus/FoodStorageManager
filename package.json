{
  "name": "foodstoragemanager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "npm run install-all && npm run schema && concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && npm --workspace=client install && npm --workspace=server install && npm --workspace=schema install",
    "client": "npm --workspace=client run dev",
    "server": "npm --workspace=server run dev",
    "schema": "npm --workspace=schema run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Accommodus/FoodStorageManager.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Accommodus/FoodStorageManager/issues"
  },
  "homepage": "https://github.com/Accommodus/FoodStorageManager#readme",
  "workspaces": [
    "server",
    "client",
    "schema"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
